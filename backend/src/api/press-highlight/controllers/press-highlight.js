'use strict';

/**
 * press-highlight controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::press-highlight.press-highlight');
